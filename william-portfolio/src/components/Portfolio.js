import './Portfolio.css';

const Portfolio = ({height}) => {

    return (
        <section className="portfolio" style={{height: height + 'px'}}>
            <p>hej</p>
        </section>
    )
}

export default Portfolio;